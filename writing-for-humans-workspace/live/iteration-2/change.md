# New baseline: ordinary task in a clean folder

Asher rejected the previous kickoff because references to versions and setup exposed the evaluation. He requested a simple task with the two skill names, in a separate temporary folder, and archived the previous thread.

The runner now creates a standalone temporary Git repo with only the two copied SKILL.md files under `.agents/skills/`. It gives the participant the topic and "Use $writing-for-humans and $unslop." The thread is named "Team invitations" and uses branch `main`. Source revisions, skill hashes, iteration numbers, and evidence stay in this authoring workspace.

This changes the participant context, so iteration 2 is a new baseline. Neither writing skill changed. The normal harness and full-access filesystem permissions still apply.
