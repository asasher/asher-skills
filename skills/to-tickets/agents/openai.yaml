interface:
  display_name: "To-Tickets"
  short_description: "Split a spec (or plan, or conversation) into backlog-ready tickets with Depends on #N blocking edges; quiz first, publish blockers-first"
  default_prompt: "Use $to-tickets to split this spec into backlog-ready tickets with blocking edges."

policy:
  allow_implicit_invocation: false
