# Spec: <name>

> High-level direction — the spec `to-tickets` splits into pickup-able tickets. Direction, not implementation:
> no file paths or code (prototype-validated snippet excepted). Delete the dev-only sections for a non-dev spec.

## Problem

<!-- What's wrong, from the user's perspective. The reason this direction was needed — not the solution. -->

## Solution

<!-- The decided direction, same perspective. What we're going to do and why, at the altitude of direction. -->

## User stories

<!-- Numbered. Cover the full surface, including edges — not just the happy path. -->

1. As a **<actor>**, I want **<feature>**, so that **<benefit>**.

## Implementation decisions

<!-- Each real choice made, with the constraint that forced it. Decisions, not a survey of dropped options.
     Prose only — no file paths or code (one exception: a prototype-validated snippet encoding a decision). -->

- **<decision>** — <the constraint that forces it>.

## Testing decisions

<!-- DEV ONLY — skip when it doesn't apply. How the work will be proven: what's worth testing, what's left
     untested, and why. Direction, not a test plan. -->

## Test seams

<!-- DEV ONLY — skip when it doesn't apply. The public seams the work will be tested at; prefer the highest
     existing seam, the fewer the better. Note what's deliberately left untested. -->

## Out of scope

<!-- What's deliberately excluded. Each line says what it is, so a reader's question is answered in place. -->

- <exclusion> — <what it is / why it's out>.

## Notes

<!-- Open questions and anything the conversation left undecided, recorded plainly. Flag gaps here rather than
     interviewing the user. -->

- <open question or undecided point>.
