interface:
  display_name: "Verify Your Work"
  short_description: "Check each claim and return reproducible evidence"
  default_prompt: "Use $verify-your-work to verify these changes and report findings."

policy:
  allow_implicit_invocation: true
