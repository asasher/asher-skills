interface:
  display_name: "Unslop"
  short_description: "Cut AI tells from any writing"
  default_prompt: "Use $unslop on any user-facing text before sending it."

policy:
  allow_implicit_invocation: true
