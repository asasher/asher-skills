interface:
  display_name: "Capture"
  short_description: "Capture the conversation's loose items — bugs, ideas, follow-ups — as minimal work-typed GitHub issues with their chat context; no readiness label, so grooming routes them"
  default_prompt: "Use $capture to capture the items this conversation has surfaced as issues."

policy:
  allow_implicit_invocation: true
