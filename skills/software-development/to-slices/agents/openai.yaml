interface:
  display_name: "To-Slices"
  short_description: "Publish an approved split as ready tickets and dependencies"
  default_prompt: "Use $to-slices to split this spec'd issue into ready child issues with blocking edges."

policy:
  allow_implicit_invocation: false
