interface:
  display_name: "To-Slices"
  short_description: "Split a spec'd issue (or spec, plan, conversation) into ready tracer-bullet child issues with blocking edges; justified recommendation the user edits before anything publishes"
  default_prompt: "Use $to-slices to split this spec'd issue into ready child issues with blocking edges."

policy:
  allow_implicit_invocation: false
