interface:
  display_name: "Serve via Tailnet"
  short_description: "Serve a local HTML artifact over the tailnet so the user can view it from any of their devices"
  default_prompt: "Use $serve-via-tailnet to serve this artifact for viewing."

policy:
  allow_implicit_invocation: true
