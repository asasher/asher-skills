interface:
  display_name: "Code Review"
  short_description: "Review standards and spec with pinned revisions and risk-scaled context"
  default_prompt: "Use $code-review to review these changes on both axes."

policy:
  allow_implicit_invocation: true
