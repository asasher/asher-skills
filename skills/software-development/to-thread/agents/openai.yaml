interface:
  display_name: "To Thread"
  short_description: "Spawn named, interactive, harness-native background sessions seeded with a standalone prompt, and tell the user how to attach"
  default_prompt: "Use $to-thread to spawn this work as an attachable background session."

policy:
  allow_implicit_invocation: true
