interface:
  display_name: "Deliver"
  short_description: "One unit of work to a review-ready PR: implement, verify and fix, PR, adversarial review, evidence; the coverage check and promotion PR for a spec issue"
  default_prompt: "Use $deliver to run this unit of work to a review-ready PR."

policy:
  allow_implicit_invocation: true
