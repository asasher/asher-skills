interface:
  display_name: "Shape"
  short_description: "Shape one ticket into an approved spec in its worktree"
  default_prompt: "Use $shape to settle this issue's decisions and publish its spec."

policy:
  allow_implicit_invocation: true
