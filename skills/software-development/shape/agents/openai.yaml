interface:
  display_name: "Shape"
  short_description: "Shape one ticket through interview, isolated research and prototypes, and a published spec"
  default_prompt: "Use $shape to settle this ticket's decisions and publish its spec."

policy:
  allow_implicit_invocation: true
