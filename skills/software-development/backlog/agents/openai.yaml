interface:
  display_name: "Backlog"
  short_description: "Groom all tickets; dispatch builds; inspect PRs and progress"
  default_prompt: "Use $backlog <verb> to run one dispatcher verb over this repo's issues."

policy:
  allow_implicit_invocation: false
