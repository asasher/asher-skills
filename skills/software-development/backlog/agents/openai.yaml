interface:
  display_name: "Backlog"
  short_description: "Groom the backlog into confirmed shaping subjects, fan ready tickets into build threads that exit to the tracker, and query the fleet with status"
  default_prompt: "Use $backlog to triage the open issues in this repo."

policy:
  allow_implicit_invocation: false
