interface:
  display_name: "Backlog"
  short_description: "Groom, build, check status, and recap what shipped in HTML"
  default_prompt: "Use $backlog <verb> to work with this repo's backlog; recap [time range] creates a readable HTML report of what shipped."

policy:
  allow_implicit_invocation: false
