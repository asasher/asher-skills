interface:
  display_name: "Backlog"
  short_description: "Groom tickets; build frontiers, specs, and milestone waves"
  default_prompt: "Use $backlog <verb> to run one lifecycle verb over this repo's issues."

policy:
  allow_implicit_invocation: false
