interface:
  display_name: "Technical Writing"
  short_description: "Durable artifacts a tired reader gets on the first read"
  default_prompt: "Use $technical-writing when writing specs, tickets, change requests, reports, or documentation."

policy:
  allow_implicit_invocation: true
