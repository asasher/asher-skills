# Merge Change

The human authorization gate. Every implementation loop ends at a review-ready PR; nothing merges until the user asks — invoking this skill _is_ that ask.

Install: `npx skills add <repo-url> --skill merge-change`.
