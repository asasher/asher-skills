interface:
  display_name: Merge Change
  short_description: Explicit approval gate that merges review-ready PRs one change at a time, in dependency order.
  default_prompt: Merge the review-ready changes I named, in dependency order, re-checking CI at merge time; resolve conflicts by documented intent and stop on any spec-versus-spec collision.
policy:
  allow_implicit_invocation: true
