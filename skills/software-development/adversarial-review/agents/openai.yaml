interface:
  display_name: "Adversarial Review"
  short_description: "Converge review and optional behavioral verification within one persisted pass budget"
  default_prompt: "Use $adversarial-review to converge this PR."

policy:
  allow_implicit_invocation: true
