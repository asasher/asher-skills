interface:
  display_name: "Adversarial Review"
  short_description: "Converge a PR to LGTM via driver-sequenced reviewer and fixer passes sharing only the PR"
  default_prompt: "Use $adversarial-review to converge this PR."

policy:
  allow_implicit_invocation: true
