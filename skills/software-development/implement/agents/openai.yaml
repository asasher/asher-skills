interface:
  display_name: "Implement"
  short_description: "Implement, check, and push coherent progress in a worktree"
  default_prompt: "Use $implement to build this issue."

policy:
  allow_implicit_invocation: true
