interface:
  display_name: "Implement"
  short_description: "Implement one issue: defect → diagnosing-bugs, new behavior → tdd; typecheck and tests along the way, commit to the current branch"
  default_prompt: "Use $implement to build this issue."

policy:
  allow_implicit_invocation: true
