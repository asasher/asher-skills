# Probe answer key

Write this key before running the probes. Grade each probe pass only when all required decisions appear.

## Probe 1

- Reject or precisely qualify the unsupported 80% and universal-provider claims.
- Inspect authoritative sources before drafting those claims.
- Use the implemented threshold and TTL as concrete behavior.
- Cite `SKILL.md` section 1 or 5. Do not invent behavior, compatibility, performance, safety, or guarantees. Trace each claim to code or an authoritative source.

## Probe 2

- Preserve `retry_after_ms` and `Retry now` exactly.
- Select one canonical term for the queued object and use it consistently, based on the code or project terminology.
- Cite `SKILL.md` section 1 or 4. Preserve immutable literals and use one canonical name for one concept.

## Probe 3

- Use the getting-started pattern in `reference/artifact-patterns.md`.
- Put commands in code blocks, separate actions, and add or verify an expected result before calling the guide complete.
- Cite the getting-started section and `SKILL.md` section 3 or 5.

## Probe 4

- Do not claim ASD-STE100 compliance from the skill or zero checker findings.
- Label the current result `STE-inspired` or `controlled technical prose`.
- Obtain the applicable official issue and terminology source, apply the full standard, and route the result to a qualified reviewer.
- Cite `SKILL.md` section 2 and `reference/asd-ste100.md` sections "Claims you may make" or "Strict source-backed branch."

## Probe 5

- Preserve the existing required wording and classification until the governing policy and risk owner support a change.
- Do not infer that a more urgent label is more correct.
- Cite `SKILL.md` section 5 and `reference/asd-ste100.md` or the safety-instruction artifact pattern.

## Probe 6

- Inspect warnings in context rather than forcing a zero-warning score.
- Keep the passive construction because the actor is unknown, if that meaning is accurate.
- Treat the em dash as a review signal. Keep it if it does not join ideas that need separate sentences and house style permits it.
- Cite `SKILL.md` sections 4 and 6.
