# <capability the slice delivers, from the user's perspective>

<!-- One tracer bullet — see reference/template-guide.md for each field. This is what gets published to the
     tracker, blockers first. -->

## Intent

<!-- One or two sentences: what this ticket makes possible and why, traceable back to the source direction. -->

## Slice

<!-- The one real thing this ticket does end to end, and how it's demoable on its own. For a wide-refactor
     ticket, name the phase: expand / migrate-batch N / contract. Prose only — no file paths or code. -->

## Done

<!-- The checkable outcome that means the slice is delivered — what a demo shows. Direction, not a test plan. -->

## Depends on

<!-- Blocking edges copied VERBATIM from the repo's recorded convention (default below, per
     backlog-policy.md § Dependencies). Omit this section for a root ticket with no blockers. These are what
     `backlog run` reads to skip blocked work. -->

- [ ] depends on #N

<!-- Work-type (optional): bug / enhancement / refactor — name it if known, else grooming sets it. -->
