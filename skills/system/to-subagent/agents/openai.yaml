interface:
  display_name: "To Subagent"
  short_description: "Dispatch one checked task in the supplied directory"
  default_prompt: "Use $to-subagent to dispatch this work to a staffed subagent."

policy:
  allow_implicit_invocation: true
