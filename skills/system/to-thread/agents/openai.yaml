interface:
  display_name: "To Thread"
  short_description: "Spawn named sessions through the outer harness, including T3 HTTP, in an exact supplied directory"
  default_prompt: "Use $to-thread to spawn this work as an attachable background session."

policy:
  allow_implicit_invocation: true
