interface:
  display_name: "Spreadsheet Loop"
  short_description: "Build complex Excel workbooks on a browser-native surface (Univer), then compile to .xlsx as the last step"
  default_prompt: "Use $spreadsheet-loop to build a workbook: iterate on the web, compile to Excel."

policy:
  allow_implicit_invocation: false
