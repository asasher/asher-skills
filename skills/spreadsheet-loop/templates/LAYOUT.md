<!-- LAYOUT.md skeleton — the VISUAL LANGUAGE: what the workbook looks like. See reference/model-vs-layout.md.
     Keep this apart from MODEL.md. This governs the snapshot; it is not the snapshot. -->

# <Workbook name> — Layout

## Sheet structure
<!-- The sheets, their order, and what occupies each region:
     title band / input block / calculation area / output summary. -->

## Placement
<!-- Where each named range from the model lands. This is where the model's vocabulary gets addresses.
     e.g. TaxRate -> Assumptions!B2 ; TotalRevenue -> Revenue!D5 -->

| Named range | Cell |
|---|---|
|  |  |

## Header & section treatment
<!-- Title rows, header rows, merges, freeze panes. -->

## Number formats
<!-- Currency / percent / thousands / dates, per column or per named range. -->

## Colour & border conventions
<!-- The SYSTEM, not one-off styling. A reader should be able to restate it:
     e.g. inputs = green fill; computed = plain; totals = bold on highlight fill; headers = brand blue. -->

- **Inputs:**
- **Computed:**
- **Totals:**
- **Headers:**

## Conditional emphasis
<!-- Where highlight-cell rules draw the eye (a total over threshold turns green). v1 supports
     highlight-cell number comparisons only — see reference/converter.md. -->
