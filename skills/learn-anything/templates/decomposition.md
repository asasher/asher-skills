# Decomposition

<!-- Grown lazily: add a node only when it's about to get a loop. A full map with zero reps is the
     "beautiful tree" failure pattern. Mark each node with its drill file once one exists. -->

- <skill>
  - <sub-skill> → `drills/<name>.md` (active)
  - <sub-skill> (no loop yet)
