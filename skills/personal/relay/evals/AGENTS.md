# Relay eval contract

## Iteration contract

- Target: `skills/personal/relay/SKILL.md` plus its directly cited references.
- Deterministic full run: `python3 skills/personal/relay/evals/test_relay.py` and `python3 skills/personal/relay/evals/test_renderer_runtime.py`.
- Done: both deterministic suites pass.

## Agent execution

Run the deterministic tests without live credentials, provider endpoints, or real recipient addresses.
