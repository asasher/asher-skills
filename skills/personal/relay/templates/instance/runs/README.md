Each communication run is immutable after review. Store its bag, rendered HTML/text, authored light/dark
previews, approval manifest, self-contained review sheet, and review-loop state together. New evidence or any
approved-field change creates a new run or explicit supersession; never patch approved bytes.
