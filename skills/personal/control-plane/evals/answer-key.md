# Control Plane — optional Until Zero answer key

| Probe | Pass condition |
|---|---|
| P1 | No. An optional sibling joins the morning run only after explicit configuration; the explicit `runway` command remains available. |
| P2 | Mark Runway failed and expose it in the brief without presenting stale data. It does not block or undo Start Work because the required Close/Pull/Ingest gates own that decision. |
| P3 | Invoke `until-zero setup` by name. Until Zero owns every listed artifact/effect; Control Plane records only its binding, cadence, phase status, and brief result. |
