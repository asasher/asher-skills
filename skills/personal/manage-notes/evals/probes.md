# Manage Notes - situated probes

Run with `SKILL.md` and only the named reference. Require file-and-sentence citations.

## Probes

**P1 - unbound capture.** The machine has voice recordings, but `docs/agents/capture-sources.md` does not exist.
May Intake use a remembered personal path?

**P2 - Opportunity query.** A question asks what pursuits mention a customer. Which roots are searched, and
which claims require wikilink citations?

**P3 - Opportunity lint without sibling.** `Opportunities/` exists but `manage-opportunities` is not installed.
Which checks still run and how is schema status reported?

**P4 - commercial inbox item.** An inbox line records client acceptance. May Ingest directly set a stage?

**P5 - archive timing.** Intake successfully pulls a capture whose binding supports deletion. May Intake
delete it immediately?
