interface:
  display_name: "Codex Imagegen"
  short_description: "Generate raster assets through proxy or Codex"
  default_prompt: "Use $codex-imagegen to create versioned flat, layered, or sprite assets. Prefer configured CLIProxyAPI, then native Codex; direct API keys require explicit permission."

policy:
  allow_implicit_invocation: true
