# Setup failure

The actual provider rollout included global Cloudflare and authoring skills alongside the six project packages. Project-scoped skill overrides did not control enablement in this runtime. The run was interrupted and preserved. No skill-quality score applies. A dedicated provider instance supplies the same path-based exclusions as startup flags for the next fresh thread. The source skill revision and production prompt stay unchanged.
