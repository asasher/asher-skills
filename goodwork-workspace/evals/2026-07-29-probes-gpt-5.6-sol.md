# gpt-5.6-sol probe answers, 2026-07-29 (codex exec, read-only, isolated ctx)

## P1
Treat `goodwork` as a no-argument invocation: read the existing profile, summarize its snapshot plainly, show the board, and suggest one concrete, time-boxed next step. Do not restart the interview.

Deciding file: `SKILL.md`:

> “No argument: if `goodwork/PROFILE.md` exists, summarize its snapshot in plain language, show the board, and suggest the next step.”

## P2
Do not move Acme yet. Start the short-form interview to create `goodwork/PROFILE.md`; after that, resume the `track` stage change.

Deciding file: `SKILL.md` — “**Guard: `scout`, `track`, and `assets` never run against a missing `goodwork/PROFILE.md` — run the short-form interview from [reference/interviewing.md](reference/interviewing.md) first.**”

## P3
Treat “looks good, send it” as approval and send the final draft verbatim—no rewriting.

Deciding rule in [SKILL.md](/tmp/gw-probes/p3/SKILL.md): “Nothing is sent, submitted, or published until the person approves the final text in chat. The approved text is what goes out, verbatim.”

Afterward, update the opportunity record in `goodwork/opportunities.json` and regenerate `goodwork/board.html`, because: “Every session ends with the workspace updated, the board current…”

The exact fields to record—such as timestamp, channel, or stage—are genuinely ambiguous because `SKILL.md` routes outreach tracking to `reference/opportunities.md`, but that file was not provided.

## P4
Pause “harder” tailoring. At 4/8 evidenced must-haves (50%), identify the unsupported requirements and build a proof artifact before pursuing; only emphasize claims already supported by evidence.

Deciding file: `reference/opportunities.md`:

> “Below that, the honest fix is a proof artifact ([assets.md](assets.md)), not adjectives.”

Also:

> “tailoring is selection and emphasis from the same verified evidence base, never invention.”

## P5
Run the `interview` command, creating `goodwork/` when first needed.

The sibling `interview` skill owns conversational conduct; this reference owns the career-specific question map and listening frameworks.

First two sentences:

> Let’s not force one life answer yet; my first guess is that you’ll get more clarity by reacting to a few very different futures than by naming a perfect job from scratch. Start with your current path going as well as it reasonably could: five years from now, what are you doing, and what about that life feels good or wrong?

Deciding citations:

- [SKILL.md](/tmp/gw-probes/p5/SKILL.md): “"What should I do with my life" infers `interview`”
- [reference/interviewing.md](/tmp/gw-probes/p5/reference/interviewing.md): “Run the conversation through the **`interview` sibling skill**: it owns the conduct — one question at a time, hypothesis-anchored questions the person corrects rather than answering cold, dependency-ordered exploration, persistent findings.”
- Same file: “This reference supplies what the `interview` skill cannot know: the career-domain question map, and the frameworks that tell you what to listen for.”

## P6
Update `goodwork/PROFILE.md` cumulatively with the History and Energy findings, then regenerate and show `goodwork/board.html`. Do not wait until the interview ends or rewrite the profile from scratch.

Deciding file: `reference/interviewing.md`:

> “Write `goodwork/PROFILE.md` cumulatively as sections land, and at natural milestones regenerate the board and show it ([board.md](board.md)).”

Why now:

> “Watching the profile take shape is the reinforcement that keeps a tiring, personal interview going — reflect progress back: ‘here's what I've got so far — this already explains a lot.’”

## P7
Regenerate `goodwork/board.html`, summarize the current position in chat, then tell them to open the file locally. Never start a server or claim to show a preview that the session lacks.

Deciding file: `reference/board.md`:

> “Absent one, say plainly: ‘I've updated your board — open `goodwork/board.html` in your browser.’ Never stand up a server for it.”

## P8
In `PROFILE.md`:

- Revise **Strengths & interests**: “wants to manage” is no longer an unqualified aspiration. Record that a one-week manager-shadowing experiment was strongly draining, marked **tested**.
- Revisit **Snapshot** and **Search parameters** if they currently position management as the target; ask whether management is still desired, desired only under certain conditions, or no longer a target.
- Add a dated changelog entry recording the contradiction and resulting direction.

Preserve the original claim as a dated historical note, including that it was **reported**—do not silently delete it. Also preserve the concrete shadowing episode and, where possible, the person’s exact words.

Deciding file: [reference/profile.md](/tmp/gw-probes/p8/reference/profile.md)

> “For each new item … **contradict** (revise the claim, keep a dated note of what it used to say)…”

And:

> “When new evidence contradicts the profile, say so plainly and ask which is true now.”

The file does not specify the person’s answer, so the final target-role change remains genuinely ambiguous until that question is resolved.

## P9
“No—don’t connect your email or LinkedIn. This workflow doesn’t automatically monitor accounts.”

Instead, offer to track replies when the user pastes or summarizes them in chat, update each opportunity’s stage and next action, and run weekly check-ins.

Deciding file: `SKILL.md` — “`goodwork/` in the project root, created the first time any command needs it — no setup command, no accounts, no connections, no server:”

## P10
First, tighten application targeting—apply only to roles with strong fit and evidence coverage.

Do not revise evidence or application documents this week.

Deciding file: `reference/opportunities.md`:

> “applications but no screens → targeting, then evidence, then documents, in that order”

And:

> “One change per week — changing three things at once makes next week's numbers meaningless.”

