1. **P1 — Exclude the interview’s reporting feature.** It is the conversation’s deliverable and already in scope, so it does not belong in the capture list.

   **Citation — `to-backlog/SKILL.md`:** “Collect every item that is real work but not this conversation's deliverable: reported defects, requested or implied enhancements, follow-ups, questions that became work. Skip what is already tracked, already in scope here, or idle musing no one committed to.”

2. **P2 — Confirm before publishing.** I would present the three proposed tickets, including their titles, work-types, and context notes, and wait for explicit approval. “Capture those” initiates the capture but does not bypass the specified confirmation presentation.

   **Citation — `to-backlog/SKILL.md`:** “The user edits, drops, adds; **nothing publishes before they approve.**”

3. **P3 — Do not apply `ready-for-agent`.** The crash gets its work-type label but no readiness role, regardless of how clear or immediately actionable it appears.

   **Citation — `to-backlog/SKILL.md`:** “**Apply no readiness role**: an un-routed ticket is the groom sweep's intake by design.”

4. **P4 — Proposed types:**

   - CSV export crash: bug/defect.
   - Scheduled reports: enhancement.
   - Ask design about the date-picker: follow-up.

   The exact tracker label for the follow-up is ambiguous because the provided skill text does not reproduce the project’s label-role names; it requires consulting `docs/agents/backlog-policy.md`. Types are proposed during confirmation and recorded when the tickets are published.

   **Citation — `to-backlog/SKILL.md`:** “Present one compact list: per item a one-line title, the proposed work-type per the label roles (`docs/agents/backlog-policy.md` § Label roles — the type is a fact best known now, while the context is live), and a one-phrase note of what context the ticket will carry.”

   **Citation — `to-backlog/SKILL.md`:** “Create each through the tracker binding recorded in `docs/agents/platform.md`: a title, the work-type label, and a body carrying the item's chat context — the symptom and repro as reported for a bug, the motivating exchange and intent for an enhancement, links to any artifact the conversation named.”

5. **P5 — Preserve the reported symptom, reproduction information, and pasted error text.** The error may appear verbatim because reporter-provided error messages are evidence.

   **Citation — `to-backlog/SKILL.md`:** “Create each through the tracker binding recorded in `docs/agents/platform.md`: a title, the work-type label, and a body carrying the item's chat context — the symptom and repro as reported for a bug, the motivating exchange and intent for an enhancement, links to any artifact the conversation named.”

   **Citation — `to-backlog/SKILL.md`:** “**No file paths or code snippets**, with one exception: a verbatim fragment the reporter themselves gave (an error message, a stack trace, a quoted behavior) is evidence, and evidence is context.”

6. **P6 — Record the scheduled-reports ticket as a native sub-issue child of #7.** If #7 is a capstone, adding the open child re-blocks it under the open-children policy.

   **Citation — `to-backlog/SKILL.md`:** “Wire each created ticket as the parent's child per the platform playbook's recorded relation.”

   **Citation — `to-backlog/SKILL.md`:** “Use when the captures are installments of a parent's direction — e.g. gaps discovered while building one of its slices; under the backlog policy's open-children rule, attaching them re-blocks a capstone parent by itself.”

7. **P7 — Stop expanding the capture and route the detailed decomposition elsewhere.** The capture should remain report-level context, not acquire flows, edge cases, rollout sequencing, acceptance criteria, or a plan. If this has become a decided direction needing decomposition, use `to-slices` on the user’s call; otherwise it belongs in shaping.

   **Citation — `to-backlog/SKILL.md`:** “No dependency edges, no readiness audit, no slicing — a capture that needs those has outgrown capture and belongs to shaping or `to-slices`.”

   **Citation — `to-backlog/SKILL.md`:** “A capture ticket is the item plus its live context, at the altitude of a report — not acceptance criteria, not a slice, not a plan.”

8. **P8 — Do not publish anything.** State that the tracker binding is missing and ask the user how to proceed.

   **Citation — `to-backlog/SKILL.md`:** “Absent a recorded binding, state the gap and ask the user before publishing anything.”
