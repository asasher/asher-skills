# to-backlog probes — Claude subagent transcript, 2026-07-29

**P1 (sweep boundary).** No — the reporting feature is this conversation's deliverable, not a loose item, so it stays out of the capture list. Citation (to-backlog/SKILL.md): "Collect every item that is real work but not this conversation's deliverable: reported defects, requested or implied enhancements, follow-ups, questions that became work. Skip what is already tracked, already in scope here, or idle musing no one committed to."

**P2 (confirm gate).** No — I present the compact list (title, proposed work-type, context note per item) and wait for the user's approval before creating anything. Citation (to-backlog/SKILL.md): "The user edits, drops, adds; **nothing publishes before they approve.**"

**P3 (readiness).** No readiness label, however clear the item — routing to ready is grooming's judgment, not capture's. Citation (to-backlog/SKILL.md): "Readiness is a routing judgment and belongs to grooming — capture applying a readiness role would make it a second groom." (And: "**Apply no readiness role**: an un-routed ticket is the groom sweep's intake by design.")

**P4 (work-type).** CSV crash → bug; scheduled reports → enhancement; the design/date-picker reminder → a follow-up, typed with whatever name the repo's label roles give that kind of work (I'd read `docs/agents/backlog-policy.md` § Label roles to get the exact names). The type is proposed at the confirm step and recorded on the ticket at capture time, while the conversation is live. Citation (to-backlog/SKILL.md): "the proposed work-type per the label roles (`docs/agents/backlog-policy.md` § Label roles — the type is a fact best known now, while the context is live)". Also: "The work-type (bug / enhancement / whatever the label roles name) is recorded at capture because it is a fact the conversation knows."

**P5 (context fidelity).** The body carries the symptom and repro as the user reported them, plus links to any artifact the conversation named — and yes, the pasted error text goes in verbatim, as the reporter-given exception to the no-snippets rule. Citations (to-backlog/SKILL.md): "a title, the work-type label, and a body carrying the item's chat context — the symptom and repro as reported for a bug, the motivating exchange and intent for an enhancement, links to any artifact the conversation named." And: "**No file paths or code snippets**, with one exception: a verbatim fragment the reporter themselves gave (an error message, a stack trace, a quoted behavior) is evidence, and evidence is context."

**P6 (parent).** The scheduled-reports ticket is attached as a child of #7 via the platform playbook's recorded relation (native sub-issues here); if #7 is a capstone, gaining an open child re-blocks it — it becomes undispatchable again. Citations (to-backlog/SKILL.md): "**`to-backlog <parent ticket id>`** — same, but attach each published ticket as a **child** of the given parent through the parent/child relation the platform playbook records." And: "under the backlog policy's open-children rule, attaching them re-blocks a capstone parent by itself."

**P7 (scope creep).** No — flows, edge cases, and a rollout order are decomposition-grade detail; that item has outgrown capture, and I say it belongs to shaping or `to-slices` (handed there only on the user's call), keeping the capture ticket at report altitude. Citation (to-backlog/SKILL.md): "No dependency edges, no readiness audit, no slicing — a capture that needs those has outgrown capture and belongs to shaping or `to-slices`." (Boundary hand-off: "a 'capture' that is really a decided direction to decompose is handed there, on the user's call.")

**P8 (no binding).** I publish nothing — I state that no tracker binding is recorded in `platform.md` and ask the user how to proceed. Citation (to-backlog/SKILL.md): "Absent a recorded binding, state the gap and ask the user before publishing anything."
