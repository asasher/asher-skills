Build an analytics dashboard for a SaaS product called "Northstar". It should have a left sidebar with navigation, a top bar, a few key metrics, a chart area, and a recent-activity list. Make it a single self-contained index.html I can open directly in a browser — you may load Tailwind from the CDN and fonts from Google Fonts, but no build step and no external image hosts. Write the file as index.html in the current directory.
