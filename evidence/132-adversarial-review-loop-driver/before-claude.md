Executor: Claude subagent (in-session, Claude Code), 2026-07-27
Context: BEFORE text — skills/software-development/adversarial-review/{SKILL.md,reference/conduct.md}
as of main (pre-change), plus probes P1-P9. Prompt file: before-prompt.md (frozen before edits).

**P1 (reviewer's hands).** Next action: do not push the fix. Post the typo bug as a review comment anchored to its file and line, with the concrete failure it causes. Cite: `reference/conduct.md`, Reviewer — "**Never edits code.**" (Supported by SKILL.md: "post each finding as a change request comment anchored to its file and line.")

**P2 (LGTM bar).** Next action: do not post LGTM. Stop and report the open finding as unresolved. Cite: `reference/conduct.md`, Reviewer — "A cap reached with findings open is reported as unresolved, never converted to approval." (Also: "Nothing else lowers the bar — not effort spent, not iteration fatigue, not the cap approaching.")

**P3 (crash recovery).** What lets a respawned reviewer continue: the iteration-state comment persisted on the change request — role, iteration count, last-seen SHA, status, next expected actor — which is exactly what each iteration must have posted. Cite: `reference/conduct.md`, Shared rules — "After each iteration, persist state on the change request via the platform's comment verb: role, iteration count, last-seen SHA, status, next expected actor. Either side can die and be respawned from this record alone." (SKILL.md agrees: "a side that goes quiet past its timeout is respawned from the change request's persisted state (conduct § Shared rules), picking up at the next expected action.")

**P4 (product question).** Next action: stop without resolving the finding; post the product question plus the evidence on the change request and wait for a human ruling — do not invent the behavior. Cite: `reference/conduct.md`, Reviewer — "when a finding reveals a real product question — what the behavior *should* be, not whether the code does it — stop without resolving it and surface the question plus evidence on the change request for a human ruling. Only an explicit ruling goes onward. Neither role invents behavior."

**P5 (fixer disagreement).** Options are exactly two: commit a fix anyway, or reply to the comment with an explicit non-fix pushback stating why finding 3 is wrong. Silently skipping it is not an option. Cite: `reference/conduct.md`, Fixer — "each iteration addresses every actionable finding: a fix commit, or an explicit non-fix reply with the reason it's wrong. Disagreement is addressed; silence is not." (SKILL.md: "or push back in a reply with the reason a finding is wrong — disagreement is addressed too, silence is not.")

**P6 (mid-loop turn boundary).** AMBIGUOUS. The provided text assigns the orchestrating session responsibility for the loop's health but says nothing about turn boundaries, background polls, or whether the session may end its turn while children run — the only monitoring mechanics it specifies belong to the two dispatched sides via `watch-until`. Closest sentence: `SKILL.md`, Bounds — "The session running this skill owns the loop's health: a side that goes quiet past its timeout is respawned from the change request's persisted state (conduct § Shared rules), picking up at the next expected action." That sentence obliges me to remain able to detect a quiet side and respawn it, but it does not decide end-turn-with-poll versus stay-resident.

**P7 (after convergence).** Next action: stop waiting and report the loop converged now. The text defines completion as LGTM landing — the fixer's stop condition is LGTM itself, and its final report is already in hand; no "shutdown confirmation" step exists anywhere in the procedure, so waiting on one is waiting on nothing the text requires. Cite: `SKILL.md`, The fixer — "When `LGTM` lands, stop." and `reference/conduct.md`, Shared rules — "Stop on `LGTM`, the iteration cap, or a blocked watch — each an explicitly reported outcome."

**P8 (state comment SHA).** Produce it by reading the change request's actual current head — the SHA identified/observed on the platform for the head this iteration acted against — never from memory or assumption. Cite: `reference/conduct.md`, Shared rules — "Before any work: identify the change request, its ticket, branch, current head SHA, and the latest recorded reviewer state." (The state comment then records it as the "last-seen SHA" per "After each iteration, persist state on the change request … last-seen SHA …".)

**P9 (undeliverable report).** Next action: post the outcome on the change request itself — a final state/iteration comment recording role, status (pass complete, all findings addressed), last-seen SHA, and next expected actor (reviewer re-review) — then stop. The change request is the designed durable channel, so the outcome survives the broken return channel. Cite: `SKILL.md` — "The change request is their only shared state — findings, fixes, and the verdict all live in its comments and commits, so either side can die and be respawned without losing the loop." and `reference/conduct.md`, Shared rules — "After each iteration, persist state on the change request via the platform's comment verb … Either side can die and be respawned from this record alone."

Executor's own closing note: all nine probes answered; P6 marked AMBIGUOUS (turn-boundary mechanics undefined).
