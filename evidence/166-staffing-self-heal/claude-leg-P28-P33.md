# Claude executor leg — final head ccdb9f3 (opus subagent, in-session, read-only)

Method: /tmp/166-evals/probe-prompt.txt (probes P28–P33 verbatim from skills/system/staffing/evals/probes.md; answer key withheld from the executor). Executor read SKILL.md, reference/rankings-and-routing.md, reference/machine-audit.md, and evals/fixtures/self-heal-overlay.md only.

**P28 (transient recovery).** The row is past its retry-at (2026-07-30T09:00Z vs. clock 2026-08-02T10:00Z), so before it may be passed over this resolution must run the cheap invocation probe inline, right then — a bounded, side-effect-free real invocation down the Claude→Codex route. Citation, rankings-and-routing.md § Self-heal: "Whichever resolution first wants a transient row at or past its retry-at — or carrying none — runs the cheap invocation probe right then, before passing over the row: the staffing route command, a dispatcher preflight, a dispatch adapter's staffing step, whichever got there first." On the pass with `codex --version` unchanged at 99.1.0 (fixture: "a fresh `codex --version` on this fixture machine returns codex-cli 99.1.0 — unchanged, no upgrade cue"), the row is written back to the previously established state — effect-verified at write class: "A passing cheap probe restores the previously established state, effect-verified included, when the CLI version is unchanged and no upgrade cue applies — the limit gated invocation, not effects, so prior effect evidence for the same CLI version still stands." The probing session writes it, whole row, five evidence fields — CLI version codex-cli 99.1.0, timestamp, command shape (the cheap invocation probe), result (pass → effect-verified, write class), successor native Claude route — into the machine-local overlay: "Any session that ran a probe writes the row it probed, carrying the full five evidence fields; setup remains the whole-record regenerator." Yes, it may back builder work immediately — no full effect probe, no setup run, no human. Ambiguity flag: the doctrine does not say whether the restored row's timestamp is the original establishing date (2026-07-20) or the healing probe's instant; machine-audit.md's "Timestamps date the observation that established the recorded state" argues for the former, while "It writes the row with the outcome" carrying five fields argues for the latter.

**P29 (upgrade cue).** Nothing is restored. The cheap pass buys no state at all: "A cheap pass under an upgrade cue restores nothing and is not a state of its own." Before the route may back builder-role staffing, the full effect-class probe for that role — write class for builders — must run in the same inline act, and the row is written once with that outcome: "under an upgrade cue, recovery is the full effect-class probe for the role being staffed, run in the same inline act, and the row is written once with that outcome — effect-verified naming the class it cleared on a pass, unavailable with the observed class on a failure." If this session stops short of the effect probe it writes nothing and the row keeps reading unverified-probeable for the next resolution. The cue itself is doctrine, not discretion: "The upgrade cue is orthogonal and always wins: after a CLI upgrade, recovery requires the full effect class before the route backs builder work."

**P30 (durable, not self-healed).** No — the self-heal path does not apply. Claude→Gemini is `unavailable (permission denied)`, which machine-audit.md § Route classification puts in the durable set: "**Durable** — CLI absent, alias rejected, permission denied, effect denied: broken until a human decision or a setup re-run." Self-heal is scoped to transient rows only ("A transient-unavailable route heals as a side effect of normal resolution"), and the fixture row carries no retry-at ("Durable class — no retry-at"). The resolution therefore runs no probe, passes the row over, applies its recorded successor — the native Claude route — and reruns the same resolution order: "**Fallback.** If the selected route becomes unreachable, apply the recorded provider/model successor and rerun the same order." Recovery requires a human decision (fixing the rejected credential) or a setup re-run, never an inline probe.

**P31 (disabled never touched).** No, on both counts. The self-heal path may not probe the Codex→Claude row and no probe could promote it: "The disabled boundary. The self-heal path never probes an intentionally-disabled row, and a probe never promotes one; the disable lifts only when the owner says so." Machine-audit.md says the same for the audit side: "A successful probe never promotes a disabled row; the disable lifts only when the owner says so — in setup's interview or by editing the playbook — never from a probe result." Only effect-verified backs dispatch, so the resolution passes over the row and takes its recorded successor — the native Codex route — rerunning the resolution order. The disable lifts only via the owner (setup's interview or a playbook edit); a setup re-run would re-check only the cheap premise that `claude` is still installed and leave the owner's "cost, this quarter" choice standing.

**P32 (reclassify by observation).** Governing sentence: "A failed re-probe writes the class actually observed: the same transient class refreshes retry-at and stays transient; a durable class converts the row to durable unavailable." (a) Same usage-limit class naming a reset at 2026-08-09T09:00Z: the row stays transient-unavailable with a refreshed token `retry-at=2026-08-09T09:00Z`, written with the full five evidence fields, and the prior effect-verified-at-write-class evidence stays retained beside it as the restoration target — "A transient write over an established state retains that previously established state and its evidence beside the row — a restoration target, not a second state." This write does happen because a new reset was named; the write-nothing carve-out is narrower: "a re-probe whose outcome is exactly what the row records — the same class, no new reset — follows the establishing-observation rule and writes nothing." (b) A permission/auth error is in the durable set, so the row converts to durable unavailable (permission denied), no retry-at, successor native Claude route; recovery then needs a human decision or setup re-run. And no, repeated transient failures never escalate on their own: "No counters, no auto-escalation — the evidence trail carries observations, not policy state." Ambiguity flag: the doctrine does not state whether the retained prior effect-verified evidence survives the durable conversion in (b) or is dropped with the transient row.

**P33 (write authority / route loss).** The session that observed the loss writes the row itself — the same row write as any probe outcome: "Recording a mid-run route loss — the loss-recording step the worker-death recovery path names — is this same write, with the observed class and the retry-at when the failure named one." Shape: a whole row for the Claude→Codex direction carrying the full five evidence fields (CLI version observed, timestamp, command shape, result/failure class, recorded successor), classed transient-unavailable with the named reset as the machine-readable `retry-at=<UTC ISO 8601>` token per machine-audit.md, with the prior established effect-verified state retained beside it. No setup run is required and no human is in the path: "A transient-unavailable route heals as a side effect of normal resolution, never through a human or a setup run"; setup "remains the whole-record regenerator." Two constraints on the write: the overlay is last-writer-wins with no lock between same-machine sessions, and it is fail-closed — "a write that cannot be read back changes nothing." The run then follows the recorded successor (native Claude route) and re-resolves. Ambiguity flag: this file names the writing party only as "whoever probed" / the loss-recording step; whether the dying worker or its coordinator executes that step is defined in roles-and-fallback.md, which this probe did not put in scope.
