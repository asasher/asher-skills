# to-slices probes — Claude subagent transcript, 2026-07-29

**P1 (slice shape).** Recut. "All the payout models" is a horizontal layer, not a tracer bullet — I cut it into vertical slices that each do one real thing end to end. Citation: "The anti-pattern is the **horizontal layer** — 'all the models,' then 'all the logic,' then 'all the UI.' A horizontal ticket can't be demoed alone and doesn't derisk the whole path; a vertical slice proves the path end to end early and often." (reference/slicing.md, § Draft vertical slices)

**P2 (wide refactor).** The rename is ticketed as the three-phase sequence: one expand ticket, migrate-in-batches tickets (parallel once expand lands), and one contract ticket blocked on all batches. The two justifying conditions: "The trigger is **both** conditions: the change is *mechanical* (little per-site judgement) **and** *high blast radius* (touches many sites)." (reference/slicing.md, § The wide-refactor exception; the phases: "1. **Expand** — introduce the new form alongside the old, so both work at once. One ticket." ibid.)

**P3 (quiz gate).** No — I present the draft split and quiz the user first; my own judgment is not the gate. Citation: "This quiz *is* the human confirmation for the whole operation — **nothing publishes before it is approved.**" (reference/slicing.md, § Quiz the user)

**P4 (no binding).** I stop, state the gap, and ask the user how to proceed rather than publishing anywhere. Citation: "No recorded binding at all: state the gap and ask the user how to proceed — a backlog needs a tracker, so publishing waits on that decision." (reference/slicing.md, § Publish in the bound tracker's format)

**P5 (readiness).** No — approval of the split does not by itself bless the tickets; readiness stays unset unless the user explicitly asks for it on approval. Citation: "Do **not** auto-apply the readiness role on a fresh split — drafted work is not yet blessed for pickup." and "**Note the option** to apply readiness on approval — the quiz *is* a human confirmation, so a user who wants it may bless the tickets on the spot — but absent that request, leave readiness unset." (reference/slicing.md, § Readiness — leave it unset)

**P6 (edges).** Created in topological dependency order, blockers first, so every referenced blocker id already exists; since the playbook records GitHub native `blocked_by`, I write the native edge. Citations: "Topologically sort the graph; publish in that order." and "Where the playbook records the tracker's **native blocking relation** (e.g. GitHub `blocked_by`, written via the verbs in `docs/agents/platform.md`), write the native edge — it renders the blocking structure in the tracker's own UI." (reference/slicing.md, § Order and wire the edges)

**P7 (parentage).** #42's work-type converts to `capstone`, every slice is attached as its child via the platform playbook's parent/child relation, a pointer comment links the children, and its spec text is untouched. What keeps it out of the build sweep is the open-children rule reading the child relation, not any label change. Citations: "**Convert the parent to the `capstone` work-type** per the label roles (`docs/agents/backlog-policy.md` § Label roles), replacing its previous work-type." and "The relation is load-bearing: the backlog policy's open-children rule reads it, keeping the parent undispatchable while any child is open — no per-slice blocking edges are wired for this." and "The parent's spec text is never edited, and its readiness role is left as it stands — the open-children rule, not a label change, is what keeps it out of the build sweep." (reference/slicing.md, § Parent the slices)

**P8 (self-initiation).** No. I may at most note that a split looks warranted, but I do not run one unbidden. Citation: "To-slices runs only on the user's explicit call — recommending a split is someone else's move; performing one is never self-initiated." (SKILL.md, § To-Slices)
