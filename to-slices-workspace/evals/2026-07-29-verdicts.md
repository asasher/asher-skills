# to-slices — Tier 1 probe run, 2026-07-29

Re-run of `skills/software-development/to-slices/evals/probes.md` after the rename from `to-tickets` and
the parentage/capstone rework (asher-skills#137), per `docs/agents/probe-evals.md`. The reworked P7
(parentage) key was written with the rework, before any runs. Dual-executor per `docs/agents/staffing.md`
§ Repo deltas: a Claude subagent in-session (inherited session model, fable-5) and gpt-5.6-sol via
`codex exec -s read-only`. Context: `SKILL.md` + `reference/slicing.md`, per the probes header.

| probe | claude subagent | gpt-5.6-sol (codex exec) |
|---|---|---|
| P1 slice shape | pass | pass |
| P2 wide refactor | pass | pass |
| P3 quiz gate | pass | pass |
| P4 no binding | pass | pass |
| P5 readiness | pass | pass |
| P6 edges | pass | pass |
| P7 parentage (reworked) | pass — capstone convert, child attach, spec untouched, open-children rule named as the block | pass — same four elements, no wired edges |
| P8 self-initiation | pass | pass |

**Verdict: 8/8 both executors — pass bar met.** The rework's load-bearing probe (P7) held on both
executors: neither superseded the parent, wired per-slice blocking edges, nor attributed the block to a
label change.

Transcripts: `2026-07-29-probes-gpt-5.6-sol.md` (raw codex output) and
`2026-07-29-probes-claude-subagent.md` alongside this file. Executor prompt was the skill text plus the
probes file with `## Answer key` stripped.
