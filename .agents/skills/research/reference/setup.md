# Setup — project research delta

Reconcile exactly one owned artifact: `docs/agents/researching.md`.

1. Inspect existing research directories, source surfaces, citation practices, presentation conventions, and
   executor capacity. Treat existing material as project practice, not permission to rename it.
2. If the playbook is absent, copy `templates/researching.md` and replace placeholders only with verified repo
   facts. Default durable general research to `research/<slug>/`; record any project override explicitly.
3. If it exists, preserve project-owned path, source, privacy, and concurrency choices. Offer only missing or
   stale factual corrections; never replace the file wholesale or copy the bundled method into it.
4. Re-read the result and confirm it contains only project-specific bindings. Re-running setup with unchanged
   facts must produce no diff.

Do not write another skill's playbook, move existing research, or place a research deliverable in `evidence/`.

Completion criterion: the playbook names one canonical durable research root, any special workspace route,
available primary-source surfaces, concurrency limits, and presentation method; repo-owned edits remain and an
immediate rerun is idempotent.
