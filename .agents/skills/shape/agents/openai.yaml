interface:
  display_name: "Shape"
  short_description: "Settle a subject's strategic decisions: interview in frontier rounds, model the terms, research and prototype what the conversation can't settle"
  default_prompt: "Use $shape to settle this work's strategic decisions."

policy:
  allow_implicit_invocation: true
