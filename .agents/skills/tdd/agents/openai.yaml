interface:
  display_name: "TDD"
  short_description: "Red → green test-driven loop: behavior through public interfaces, tests only at pre-agreed seams, the anti-pattern list"
  default_prompt: "Use $tdd to build this behavior test-first."

policy:
  allow_implicit_invocation: true
