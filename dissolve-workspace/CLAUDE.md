# dissolve Eval Workspace

Read and follow `AGENTS.md` in this directory before working on this eval workspace. In particular,
run the Claude side as a subagent — never via `claude -p` — and get explicit approval before any
CLI fallback that would incur extra usage.
