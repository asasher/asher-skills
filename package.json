{
  "name": "asher-skills",
  "version": "0.0.0",
  "private": true,
  "description": "Installer entry point for the skills in this repo. Not published — run it straight from GitHub with `npx github:asasher/asher-skills`.",
  "scripts": {
    "format:fix": "prettier --write \"**/*.md\""
  },
  "dependencies": {
    "prettier": "^3.9.6"
  }
}
