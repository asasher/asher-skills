# Executor A — in-session isolated Claude agent (opus-4.8), answer-only

Raw answer, preserved verbatim. Executor received `probe-task.md` and the relay skill source only; no answer
key, no evals/ directory, no prior iteration.

---

P1. Discover: `setup` runs `python3 scripts/setup_instance.py <repo-root> --discover`, whose report inspects "repositories, project registries, trackers, release evidence, mailbox sources, existing editorial/template material, optional source skills, Node/npm, root `.env`, and AgentMail CLI reachability" (reference/setup.md), so it finds the two projects, the Git release evidence, and the task tracker. May materialize: "Setup studies the consumer repository, confirms its choices, and materializes only `relay/` plus `docs/agents/relay.md`." (reference/setup.md); "Missing defaults are copied." (reference/setup.md), and the version-pinned renderer only "when missing and validates Node 20+ and npm 10+" (reference/rich-email-contract.md). Must confirm: the choices listed in reference/setup.md step 2 — "authoritative evidence providers for shipped, pending, cash, growth, and commitments; projects and their section recipes; audience membership, explicit To/CC roles, interests, disclosure, cadence, and coalescing; operator CC; editorial rules; template choice; AgentMail inbox, verified sender/domain, and webhook or manual reconciliation." Blocked: credential/live check is blocked because "Before a live check require `.env` to be ignored by Git and mode `0600`." (reference/setup.md) — the root `.env` is not ignored and is `0644`, and Relay must "require the file Git-ignored and mode `0600`. Never source it" (SKILL.md); and because no sender is chosen, live send is blocked — "A selected unverified sender blocks live send." (reference/setup.md). Ambiguity: with no sender chosen there is no verified sender, so the conservative action is to finish local setup (discovery, binding, selection/render/review can proceed) while leaving live send blocked and never touching the `.env` until it is Git-ignored and `0600`.

P2. Allowed next actions: select, render, and present review only — "A scheduled invocation may select, render, and present review, but performs zero provider writes until the current exact review is approved." (SKILL.md), producing "exactly one validated immutable bag or explicit exclusion per eligible audience; never combine audiences." (SKILL.md), so one bag for the external audience and one for the internal digest. Provider writes: zero — "Zero provider writes without an approving `review-loop` verdict (`approve` / `approve_with_nits`) for the exact current sheet hash." (SKILL.md). Ledger facts: append-only workflow transitions for selected/rendered/reviewed, and "Append facts; never rewrite history." (reference/lifecycle-ledgers.md); no draft-created or send-submitted may be appended. Watermark: no advance — "Advance a watermark exactly once after matching `message.sent` or unique sent-message reconciliation." (reference/lifecycle-ledgers.md), and "A watermark advances only on confirmed send." (SKILL.md).

P3. For all five cases the next concrete action is identical: append `superseded`, re-render, and re-review, and AgentMail may NOT be invoked — "Any change to HTML, text, sender, To, CC, or template identity invalidates authorization: append `superseded`, re-render, and re-review." (SKILL.md), and "The only valid next action is append `superseded`, rebuild the sheet, and obtain a new verdict; zero AgentMail commands are allowed on mismatch." (reference/review-and-approval.md). So: (1) HTML change, (2) text change, (3) sender change, (4) To change, (5) CC change each require superseded + re-render + re-review with no AgentMail write.

P4. Identity reused: the same deterministic client/draft identity, `relay-<sha256>` derived from the approved canonical manifest — "On retry, derive the same client ID and read append-only workflow state." (reference/provider-adapter.md), and "One deterministic client/draft identity per approved manifest. Retries reuse it" (SKILL.md). Operation that may be retried: only the deterministic draft create — "A create timeout repeats only deterministic create." (reference/provider-adapter.md). Must never be minted: a new identity (and no resend/reply) — "Never mint a new identity, resend, or reply automatically." (SKILL.md).

P5. State appended: `blocked-ambiguous` — "If uniqueness is unavailable, append `blocked-ambiguous`; never create a new identity or resend automatically." (reference/provider-adapter.md), and "Ambiguous correlation appends a blocked workflow fact and waits for reconciliation" (reference/lifecycle-ledgers.md). Forbidden provider action: creating a new identity or resending — "never create a new identity or resend automatically." (reference/provider-adapter.md); it "waits for reconciliation; it never triggers a new send identity." (reference/lifecycle-ledgers.md).

P6. Workflow result: `sent` on the matched `message.sent`. Recipient results: A is receiving-server-delivered and B is bounced, and they stay that way — "mixed recipient outcomes remain mixed." (reference/lifecycle-ledgers.md). All-delivered: false — "'All delivered' is true only when every intended recipient has a receiving-server delivery event and none has a failure fact." (reference/lifecycle-ledgers.md). Watermark timing: advances exactly once on the `message.sent`, and the bounce does not retract it — "Advance a watermark exactly once after matching `message.sent`... Draft creation, send submission, delivery, bounce, complaint, rejection, or reply never independently advances it." (reference/lifecycle-ledgers.md). Out-of-order/duplicate events are safe: "an already-recorded event ID is ignored" (reference/lifecycle-ledgers.md). Relay does not resend — "Later failures remain visible follow-up." (reference/lifecycle-ledgers.md) and "never... resend... automatically." (SKILL.md); the bounce is visible follow-up only.

P7. Appended: a reply fact in `replies.jsonl` keyed by provider event, message, thread, and source communication — "`replies.jsonl` — reply-received facts keyed by provider event, message, thread, and source communication" and "A received reply appends a reply fact and never generates a reply." (reference/lifecycle-ledgers.md). Follow-up: none automatic; Relay sends nothing in response — "never generates a reply" (reference/lifecycle-ledgers.md), "Never mint... reply automatically." (SKILL.md). Tracking described as manual reconciliation, not real time — with no configured/effect-verified receiver, "without an effect-verified receiver, lifecycle is manual reconciliation, never real time." (SKILL.md), and reconciliation mode is recorded `manual` (reference/setup.md).

P8. Preserve: the consumer's modified files and templates — "Existing consumer files and templates are preserved." (reference/setup.md), and "The consumer owns brand name, mark, accent, typography, footer, wording, and extensions; setup preserves edits" (reference/rich-email-contract.md) — so accent, footer, and renderer edits are kept. Emit: the changed package default becomes an upgrade candidate, not an overwrite — "Changed defaults produce `.setup-candidate` files." (reference/setup.md) and "emits upgrade candidates instead of overwriting." (reference/rich-email-contract.md); the new evidence source likewise "A changed discovery report becomes a candidate; it never rewrites a confirmed binding." (reference/setup.md). Require before the new source affects selection: it must be confirmed and written into the binding (re-run setup with the binding), because "A missing or stale binding blocks that audience; it never falls back to prompt lore." (reference/selection.md) and the changed discovery report "never rewrites a confirmed binding." (reference/setup.md).

P9. Setup may read from repository discovery, the repository-root `.env`, and `relay/`, and may write only `relay/` plus `docs/agents/relay.md` — "materializes only `relay/` plus `docs/agents/relay.md`." (reference/setup.md) and "Project: `docs/agents/relay.md`, the repository-root `.env`, and `relay/` only." (SKILL.md). The old `control-plane/communications/` tree is left untouched — never read or written, and no migration occurs — "Never read or write a previous communications instance." (SKILL.md) and "It has no previous-instance migration branch." (reference/setup.md); with no owner-requested migration, the conservative action is to leave the old tree in place and ignore it.
