# Executor A — in-session isolated Claude agent (opus-4.8), answer-only

Raw answer, preserved verbatim. Executor received `probe-task.md` and the relay skill source only (including
the post-iteration-2 `lifecycle-ledgers.md` fix); no answer key, no evals/ directory, no prior iteration.

---

P1 — Discovery: `scripts/setup_instance.py <repo-root> --discover` reports "repositories, project registries, trackers, release evidence, mailbox sources, existing editorial/template material, optional source skills, Node/npm, root `.env`, and AgentMail CLI reachability" (setup.md) — here the two projects, Git release evidence, and task tracker; no Relay instance found. Setup may materialize only `relay/` plus `docs/agents/relay.md` ("materializes only `relay/` plus `docs/agents/relay.md`", setup.md), copying missing defaults and the version-pinned renderer when absent ("Missing defaults are copied"; rich-email-contract.md "copies the version-pinned renderer into the consumer instance only when missing"). Choices that must be confirmed: "authoritative evidence providers for shipped, pending, cash, growth, and commitments; projects and their section recipes; audience membership, explicit To/CC roles, interests, disclosure, cadence, and coalescing; operator CC; editorial rules; template choice; AgentMail inbox, verified sender/domain, and webhook or manual reconciliation" (setup.md). What remains blocked: the live credential/capability check — "Before a live check require `.env` to be ignored by Git and mode `0600`" (setup.md), and this `.env` is un-ignored and `0644`, so the live check is blocked (conservative action: do not read/provision the key or touch the provider until the file is Git-ignored and `0600`). With no AgentMail sender chosen, "A selected unverified sender blocks live send" (setup.md) — live send stays blocked, while local selection, rendering, and review may still proceed.

P2 — Allowed next actions: select, render, and present the review only — "A scheduled invocation may select, render, and present review, but performs zero provider writes until the current exact review is approved" (SKILL.md). Provider writes: zero (same sentence). One validated immutable bag or explicit exclusion per audience, never combined ("never combine audiences", SKILL.md Run §3) — so the external bag and the internal digest bag stay separate. Ledger facts: `selected`/`rendered` may be appended, but no `reviewed` — "`reviewed` records an approving verdict, never mere presentation" (lifecycle-ledgers.md), and no verdict exists. Watermark behavior: no advance — "A watermark advances only on confirmed send" (SKILL.md) / "Advance a watermark exactly once after matching `message.sent` or unique sent-message reconciliation" (lifecycle-ledgers.md).

P3 — All five cases invalidate the approval; the next concrete action for each is identical: append `superseded`, re-render, and re-review — "Any change to HTML, text, sender, To, CC, or template identity invalidates authorization: append `superseded`, re-render, and re-review" (SKILL.md), reinforced by "Changing HTML, text, sender, To, CC, template identity, evidence manifest, or the review sheet invalidates authorization. The only valid next action is append `superseded`, rebuild the sheet, and obtain a new verdict" (review-and-approval.md). AgentMail may NOT be invoked in any of the five cases — "zero AgentMail commands are allowed on mismatch" (review-and-approval.md).

P4 — Identity reused: the one deterministic client/draft identity derived from the approved manifest — "One deterministic client/draft identity per approved manifest. Retries reuse it" (SKILL.md); "derive the same client ID and read append-only workflow state" (provider-adapter.md). Operation that may be retried: only the deterministic draft create — "A create timeout repeats only deterministic create" (provider-adapter.md). What must never be minted: a new client/draft identity — "Never mint a new identity, resend, or reply automatically" (SKILL.md); a new send/resend is likewise excluded here since no draft ID was received.

P5 — State appended: because send submission may have reached the provider and there is no unique lookup result, append `blocked-ambiguous` — "If uniqueness is unavailable, append `blocked-ambiguous`" (provider-adapter.md); "Ambiguous correlation appends a blocked workflow fact and waits for reconciliation" (lifecycle-ledgers.md). Forbidden provider action: creating a new identity or resending automatically — "never create a new identity or resend automatically" (provider-adapter.md); the conservative action is to wait for reconciliation, appending no new send.

P6 — Workflow result: `sent` on the matching `message.sent`. Recipient A: `receiving-server-delivered`; recipient B: `bounced` (delivery.jsonl outcomes, lifecycle-ledgers.md). All-delivered result: false — "'All delivered' is true only when every intended recipient has a receiving-server delivery event and none has a failure fact" (lifecycle-ledgers.md), and B has a failure fact. Out-of-order/duplicate handling: safe — "an already-recorded event ID is ignored, and mixed recipient outcomes remain mixed" (lifecycle-ledgers.md). Watermark timing: advances exactly once on the `message.sent` match and not on delivery or bounce — "Advance a watermark exactly once after matching `message.sent`... Draft creation, send submission, delivery, bounce, complaint, rejection, or reply never independently advances it" (lifecycle-ledgers.md). Relay does not resend — "Never mint a new identity, resend, or reply automatically" (SKILL.md); the bounce is "visible follow-up" (lifecycle-ledgers.md).

P7 — Appended: a reply fact in `replies.jsonl` keyed by provider event/message/thread/source — "A received reply appends a reply fact and never generates a reply" (lifecycle-ledgers.md). Follow-up: none automated; Relay sends nothing — same sentence, "never generates a reply," and "Never mint a new identity, resend, or reply automatically" (SKILL.md). With no durable webhook receiver, tracking is manual reconciliation, not real time — "without an effect-verified receiver, lifecycle is manual reconciliation, never real time" (SKILL.md); "Do not claim real-time events unless a signature-verified receiver was effect-tested; otherwise record `manual`" (setup.md).

P8 — Preserves: consumer files and modified templates byte-identically — "Setup preserves consumer files and modified templates" (SKILL.md); "setup preserves edits and emits upgrade candidates instead of overwriting" (rich-email-contract.md), covering the edited accent, footer, and renderer. Emits: the changed package default template version as a candidate, not an overwrite — "Changed defaults produce `.setup-candidate` files" (setup.md); and the new evidence source as a discovery candidate — "A changed discovery report becomes a candidate; it never rewrites a confirmed binding" (setup.md). Requires before the new source affects selection: it must be confirmed and written into the binding — "A missing or stale binding blocks that audience; it never falls back to prompt lore" (selection.md), so the source affects selection only once the operator confirms it into the binding JSON (setup.md discover-and-bind); conservative action is to leave selection unaffected until then.

P9 — Setup may read and write only `relay/` plus `docs/agents/relay.md` (and read only `AGENTMAIL_API_KEY` from the root `.env`) — "materializes only `relay/` plus `docs/agents/relay.md`" (setup.md); "All consumer policy... live under `relay/`... the project binding lives in `docs/agents/relay.md`" (SKILL.md). The old `control-plane/communications/` tree is left untouched — never read or written and not migrated: "Never read or write a previous communications instance" (SKILL.md) and "It has no previous-instance migration branch" (setup.md); with no owner-requested migration, the conservative action is to ignore the old tree entirely.
